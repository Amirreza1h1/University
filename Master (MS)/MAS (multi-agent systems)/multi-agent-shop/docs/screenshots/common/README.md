# Common Screenshots

These screenshots apply to the project as a whole rather than one runtime scenario. They remain manual until real PNG files are placed in this directory.

| Filename | What must be visible | Status |
|---|---|---|
| `01-vscode-project-structure.png` | VS Code Explorer showing `pom.xml`, `lib/jade.jar`, `scripts`, all Java packages, tests, and documentation. | Manual; not captured |
| `02-maven-tests-success.png` | Terminal showing `mvn clean test`, 20 tests, zero failures/errors, and `BUILD SUCCESS`. | Manual; not captured |
| `03-purchase-service-tests.png` | `PurchaseServiceTest.java` and preferably the passing Java Test Runner view. | Manual; not captured |

Do not create placeholder or blank PNG files. Capture readable images and avoid exposing unrelated windows or sensitive paths.
